<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.synapse.crm</groupId>
    <artifactId>synapse-crm-base</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>crm-app</artifactId>
  <name>Synapse CRM :: Aplicacao</name>
  <description>Composicao e configuracao da instancia. Unico modulo executavel.</description>

  <dependencies>
    <!-- Composicao: a aplicacao conhece todos os modulos; nenhum modulo a conhece. -->
    <dependency>
      <groupId>com.synapse.crm</groupId>
      <artifactId>crm-shared-kernel</artifactId>
    </dependency>
    <dependency>
      <groupId>com.synapse.crm</groupId>
      <artifactId>crm-core</artifactId>
    </dependency>
    <dependency>
      <groupId>com.synapse.crm</groupId>
      <artifactId>crm-atendimento</artifactId>
    </dependency>
    <dependency>
      <groupId>com.synapse.crm</groupId>
      <artifactId>crm-automacao-config</artifactId>
    </dependency>
    <dependency>
      <groupId>com.synapse.crm</groupId>
      <artifactId>crm-campanhas</artifactId>
    </dependency>
    <dependency>
      <groupId>com.synapse.crm</groupId>
      <artifactId>crm-equipe</artifactId>
    </dependency>
    <dependency>
      <groupId>com.synapse.crm</groupId>
      <artifactId>crm-relatorios</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <!-- E09a: AuditoriaAspect. Ja chegava transitivo via crm-atendimento; explicito para nao
         depender de um modulo continuar trazendo isso no futuro. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-aop</artifactId>
    </dependency>
    <!-- E07 §2: OpenAPI gerado a partir dos @RestController — documentacao vira saida de build. -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>2.6.0</version>
    </dependency>

    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Espera por condicao em vez de corrida com jobs assincronos (PublicadorDaOutbox
         roda por fixedDelay) — ver CanalWhatsAppIT. Versao gerenciada pelo BOM do
         Spring Boot. -->
    <dependency>
      <groupId>org.awaitility</groupId>
      <artifactId>awaitility</artifactId>
      <scope>test</scope>
    </dependency>
    <!--
      ArquiteturaTest roda aqui porque crm-app e o unico modulo cujo classpath
      contem todos os demais: uma suite so cobre a regra de dependencia do
      monorepo inteiro, sem duplicar o teste em cada modulo.
    -->
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <!--
            Sem classificador, o repackage substitui o artefato do modulo pelo fat
            jar (classes em BOOT-INF/classes) e o failsafe, que roda depois do
            package, perde as classes do proprio modulo do classpath.
            Com o classificador, o jar executavel e crm-app-<versao>-exec.jar e o
            jar simples continua existindo para os testes de integracao.
          -->
          <classifier>exec</classifier>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>repackage</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
