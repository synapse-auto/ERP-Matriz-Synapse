import { Placeholder } from "@/components/shell/placeholder";

/** Cobre toda rota de menu sem tela própria ainda — nenhuma delas é escopo desta etapa (E10). */
export default function PaginaEmConstrucao() {
  return <Placeholder />;
}
